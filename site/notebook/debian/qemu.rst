qemu
====

Install::

  sudo apt-get install \
    qemu \
    qemu-utils \
    qemu-system-x86

cloud-utils
-----------

Related to qemu are the cloud-init utilities::

  sudo apt-get install \
    cloud-utils
